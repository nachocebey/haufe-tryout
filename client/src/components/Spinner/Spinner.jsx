// Spinner.js
import "./Spinner.scss"; // Asegúrate de tener un archivo CSS para estilizar el spinner

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
